// always compare same type of data type

console.log("2" > 1)
console.log("02" > 1)

console.log(null > 0)  // (>) operator only works with number so null is converted to a number (0)

console.log(null == 0)   // in this null is equal to undefined so it is false

console.log(null >= 0) // (>) this operator only works with number so null is converted to a number that is 0 